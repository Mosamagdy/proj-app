import { createTheme } from '@mui/material/styles';

// إنشاء theme مع الخط المخصص
const theme = createTheme({
  typography: {
    fontFamily: '"Inter", sans-serif', // تعيين الخط الذي تريده هنا
  }});
  export default theme;